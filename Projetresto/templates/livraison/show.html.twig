{% extends 'base.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block container %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idlivraison</th>
                <td>{{ livraison.idlivraison }}</td>
            </tr>
            <tr>
                <th>Fraisdelivraison</th>
                <td>{{ livraison.fraisdelivraison }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('indexliv') }}">back to list</a>

    <a href="{{ path('app_livraison_edit', {'idlivraison': livraison.idlivraison}) }}">edit</a>

    {{ include('livraison/_delete_form.html.twig') }}
{% endblock %}
