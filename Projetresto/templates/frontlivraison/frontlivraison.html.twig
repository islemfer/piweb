{% extends "frontlivraison/baseFront.html.twig"%}
{% block title %} Livraison {%endblock%}
{% block container %}




  <!-- book section -->
  <section class="book_section layout_padding">
    <div class="container">
      <div class="heading_container">
        <h2>
          Livraison
        </h2>
  {{form_start(form)}}
  {{form_errors(form)}}
  {{form_widget(form)}}
  {{form_end(form)}}
      
      </div>
    </div>
  </section>
  <!-- end book section -->
{% endblock %}
  
  